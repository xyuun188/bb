# 整改执行与验收报告

**报告时间：** 2026-08-28（线上证据时间以 UTC 时间戳为准）  
**代码版本：** `dbba5c9` (`fix: gate resource-exhausted training retries`)  
**发布方式：** 本地全量验收通过后执行 `scripts/sync_to_online_server.py --split-services`  经过分服务重启和只读健康检查  
**交易原则：** 继续保持 `paper`；`live_ml_ready=false` 或 `can_open_new_entries=false` 时不得开新仓。

## 1. 本次已完成的代码整改

1. 训练状态增加资源错误识别、退避、连续失败熔断、输入指纹变化重试和旧状态迁移；`MemoryError` 不再无限重试。
2. 主要训练入口统一执行持久化 training gate，避免同一模型重复运行或通过 `force` 绕过资源熔断。
3. 策略学习事件查询设置返回上限，Dashboard 策略学习增加持久化最近成功快照；watermark 或查询超时时返回 `stale_snapshot`，不返回空 scheduler 误导页面。
4. Dashboard 的 ML 状态缓存按数据库、服务实例和计数函数隔离，避免跨服务或测试数据串缓存。
5. 新增训练 gate、资源错误恢复和 Dashboard stale fallback 回归测试。

## 2. 本地验收结果

| 检查项 | 结果 |
| --- | --- |
| 全量 pytest | `3670 passed, 4 skipped, 1 warning` |
| Ruff | 通过 |
| `compileall services web_dashboard scripts` | 通过 |
| `git diff --check` | 通过 |
| 关键回归集 | `434 passed` |
| 工作区 | clean |
| Git 推送 | `origin/main` 已包含 `dbba5c9` |

允许的 4 个 skipped 仅为已有环境依赖跳过，不影响本次训练 gate 和 Dashboard 回归集。

## 3. 线上只读验收结果

最近一次只读检查时间：

- 模型服务检查：`2026-08-28T16:39:13Z`。
- 分析覆盖检查（30 分钟窗口）：`2026-08-28T16:39:13Z`。
- 策略/交易执行检查（60 分钟窗口）：`2026-08-28T16:45:55Z`。

### 已确认通过

- 三个 Phase 3 LLM 服务和量化模型 API 均为 active/可访问。
- 量化模型处于 `canary`/`shadow_observation`，Artifact 和 metadata 可加载，版本指纹可读。
- 最新 60 分钟执行合同检查的 `contract_violation_count=0`，保护单覆盖缺口为 0。
- 训练 scheduler heartbeat 正常，无 stale scheduler 或超时任务。
- 六个 `local_ai` 资源失败模型已迁移为 `resource_blocked`，`retry_count=15`，并设置 6 小时熔断窗口；不再继续高频重试。
- 注册表库存口径可拆分为 15 个：可训练 8 个、推理/评估 7 个，不再把库存数误报为并行训练数。

### 当前仍未通过

1. **实盘权限：No-Go。** `live_ml_ready=false`、模型 `production_permission=false`；模型仍为 paper-only。当前模型费后收益、收益置信下界和 Profit Factor 未同时达标。
2. **新开仓权限：No-Go。** OKX 对账仍为 `critical`，存在未解决执行合同/事实关注项；`can_open_new_entries=false`。不能用页面、配置或脚本强制打开。
3. **连续性观察：未完成。** 最近 30 分钟最大分析间隔约 289 秒，覆盖窗口仍有待处理候选；交易、Dashboard、模型隧道刚在约 `16:28Z` 重启，无法据此证明 24 小时连续运行。
4. **策略学习查询：需观察。** 60 分钟只读检查中出现一次 scheduler watermark/query timeout；接口会返回超时或最近成功快照，但仍需在连续观察窗口内证明 P95/P99 达标。
5. **训练晋级：No-Go。** 当前 challenger/canary Artifact 虽可读取，但样本外费后收益、LCB、回撤和市场状态稳定性尚未满足生产晋级合同；不得写入 `live_ml_ready=true`。

## 4. 发布决策

**代码发布：GO。** 本次代码已通过本地全量测试、静态检查、差异检查和线上只读 smoke，并已同步线上。  
**Paper 观察：GO（受控）。** 保留 paper 采样和只读 Dashboard；遇到资源过载、对账异常或分析间隔超阈值时继续自动降级。  
**Live 新开仓：NO-GO。** 对账、连续性和模型晋级证据尚未全部满足，系统必须保持 fail-closed。  
**稳定盈利结论：NO-GO。** 当前真实成交样本和费后风险证据不足，不能据此承诺盈利。

## 5. 下一次复核门槛

从本次部署窗口重新计时，至少完成：

1. 连续 24 小时：服务无非计划重启、交易心跳小于 120 秒、最大分析间隔不超过 180 秒、无重复分析、无新的 MemoryError、Dashboard 菜单点击无卡死。
2. 连续 72 小时：上述指标在三个自然周期内重复通过，OKX 对账新增未解决项为 0，收益按毛盈亏/手续费/滑点/已结算资金费平衡。
3. 模型评审：challenger 在冻结样本外同时优于 active 和 baseline，费后收益 LCB、Profit Factor、回撤、尾部风险和多空/市场状态拆分全部达标。
4. 只有以上条件全部满足，才可重新提交 Go/No-Go 签字单；在此之前不得恢复 live 新开仓。

